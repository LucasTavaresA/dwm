diff --git a/config.def.h b/config.def.h
index 53bb552..ee8c923 100644
--- a/config.def.h
+++ b/config.def.h
@@ -7,6 +7,7 @@ static const int showbar = 1;           /* 0 means no bar */
 static const int topbar = 1;            /* 0 means bottom bar */
 static const int splitstatus = 1;        /* 1 for split status items */
 static const char *splitdelim = ";";       /* Character used for separating status */
+static const char *ssplitdelim = "^";       /* Character used for separating status */
 static const unsigned int systraypinning = 0;   /* 0: sloppy systray follows selected monitor, >0: pin systray to monitor X */
 static const unsigned int systrayonleft = 0;   /* 0: systray in the right corner, >0: systray on left of status text */
 static const unsigned int systrayspacing = 0;   /* systray spacing */
diff --git a/dwm.c b/dwm.c
index 57b3833..ade6a50 100644
--- a/dwm.c
+++ b/dwm.c
@@ -968,8 +968,10 @@ void drawbar(Monitor *m) {
   unsigned int i, occ = 0, urg = 0;
   Client *c;
 	char *mstext;
+	char *msstext;
 	char *rstext;
 	int msx;
+	int mssx;
 
   if (!m->showbar)
     return;
@@ -1023,8 +1025,11 @@ void drawbar(Monitor *m) {
 		rstext = strdup(stext);
 		if (splitstatus) {
 			mstext = strsep(&rstext, splitdelim);
+			msstext = strsep(&mstext, ssplitdelim);
 			msx = (m->ww - TEXTW(mstext) + lrpad) / 2; /* x position of middle status text */
+			mssx = (m->ww - TEXTW(msstext) + lrpad) / 4; /* x position of middle status text */
 			drw_text(drw, msx, 0, TEXTW(mstext) - lrpad, bh, 0, mstext, 0);
+			drw_text(drw, mssx, 0, TEXTW(msstext) - lrpad, bh, 0, msstext, 0);
     }
 		tw = TEXTW(rstext) - lrpad + 2; /* 2px right padding */
 		drw_text(drw, m->ww - tw - stw, 0, tw, bh, 0, rstext, 0);
